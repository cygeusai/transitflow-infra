# Transit & Flow — environment contract (NO real secrets here)
# Real values live in Supabase Vault and platform env settings, referenced by name at runtime.

# --- Supabase (public, safe for the client) ---
SUPABASE_URL=https://kjooyhvynkzuvsixsutt.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# --- Supabase (server only; never ship to the browser) ---
SUPABASE_SERVICE_ROLE_KEY=set-in-edge-function-env-only

# --- Secrets stored in Supabase Vault (names, not values) ---
# qb_client_id, qb_client_secret, qb_refresh_token   # QuickBooks
# hcp_api_key                                        # Housecall Pro
# twilio_account_sid, twilio_auth_token              # Twilio SMS
# clickup_token                                      # ClickUp
# slack_bot_token                                    # Slack
# stripe_secret_key, stripe_webhook_secret           # Stripe rent payments

# --- App ---
APP_URL=https://goqtf.lovable.app
